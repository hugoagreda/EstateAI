/node_modules
/.next
/out
/build
.DS_Store
*.pem
.env
.env*.local
.vercel
*.tsbuildinfo
next-env.d.ts
# Real embeddings are generated by `npm run seed` (needs OpenAI + Supabase).
# Never commit them; they are large and environment-specific.
/data/embeddings.json
